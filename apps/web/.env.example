# Backend API Configuration
# Set this to connect to the real backend API
# If not set, the app will use mock data
VITE_API_URL=http://localhost:8080

# Development Authentication Token
# Get this from your backend administrator
# The backend requires a Bearer token for all authenticated endpoints
VITE_API_TOKEN=your-dev-token-here
